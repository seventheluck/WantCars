// const URI = 'wantcars-0-3-snapshot.m6kw7wztz4.us-west-2.elasticbeanstalk.com';
const URI = 'localhost';
const PORT = '8090';
const PROTO = 'http';

const URL = PROTO + '://' + URI + ':' + PORT;

export default URL;